name: CI

on: [push]

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [12]

    steps:
    - uses: actions/checkout@v1
    - name: Print versions
      run: |
        node --version
        npm --version
    - name: Get CPU information
      run: cat /proc/cpuinfo
