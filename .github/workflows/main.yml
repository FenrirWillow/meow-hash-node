name: CI

on: [push]

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [12]

    steps:
    - uses: actions/checkout@v1
    - name: Print versions
      run: |
        node --version
        npm --version
    - name: Get CPU information
      run: cat /proc/cpuinfo
    - name: Install build essentials
      run: |
        apt-get update
        apt-get install -y --no-cache build-essential
    - name: Print versions
      run: gcc --version
